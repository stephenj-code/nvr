mqtt:
  enabled: false

go2rtc:
  streams:
    front-west:
      - rtsp://{FRIGATE_RTSP_USER}:{FRIGATE_RTSP_PASSWORD}@192.168.1.45/cam/realmonitor?channel=1&subtype=0
    front-west-sub:
      - rtsp://{FRIGATE_RTSP_USER}:{FRIGATE_RTSP_PASSWORD}@192.168.1.45/cam/realmonitor?channel=1&subtype=1
    front-east:
      - rtsp://{FRIGATE_RTSP_USER}:{FRIGATE_RTSP_PASSWORD}@192.168.1.232/cam/realmonitor?channel=1&subtype=0
    front-east-sub:
      - rtsp://{FRIGATE_RTSP_USER}:{FRIGATE_RTSP_PASSWORD}@192.168.1.232/cam/realmonitor?channel=1&subtype=1

detectors:
  default:
    type: tensorrt
    device: 0

model:
  path: /config/model/yolov8m.engine
  input_tensor: nchw
  input_pixel_format: rgb
  width: 640
  height: 640

database:
  path: /data/frigate.db

ffmpeg:
  hwaccel_args: preset-jetson-h264

record:
  enabled: true
  alerts:
    retain:
      days: 14
  detections:
    retain:
      days: 14
  continuous:
    days: 7
  motion:
    days: 7

snapshots:
  enabled: true
  retain:
    default: 14

objects:
  track:
    - person
    - car
    - motorcycle
    - bus
    - truck
    - cat
    - dog
    - bird
  filters:
    cat:
      min_score: 0.5
      threshold: 0.7
    dog:
      min_score: 0.4
      threshold: 0.6
    bird:
      min_score: 0.5
      threshold: 0.7
      min_area: 1500  # ignore tiny/distant birds

cameras:
  front-west:
    live:
      streams:
        front-west: front-west
    ffmpeg:
      inputs:
        - path: rtsp://127.0.0.1:8554/front-west
          input_args: preset-rtsp-restream
          roles:
            - record
        - path: rtsp://127.0.0.1:8554/front-west-sub
          input_args: preset-rtsp-restream
          roles:
            - detect
    detect:
      enabled: true
      width: 704
      height: 480
      fps: 5
    motion:
      mask:
        - 0,0,704,50

  front-east:
    live:
      streams:
        front-east: front-east
    ffmpeg:
      inputs:
        - path: rtsp://127.0.0.1:8554/front-east
          input_args: preset-rtsp-restream
          roles:
            - record
        - path: rtsp://127.0.0.1:8554/front-east-sub
          input_args: preset-rtsp-restream
          roles:
            - detect
    detect:
      enabled: true
      width: 704
      height: 480
      fps: 5
    motion:
      mask:
        - 0,0,704,50      # timestamp overlay (top-right)
        - 0,440,80,480    # IPC label (bottom-left)

classification:
  custom:
    Hunter:
      enabled: true
      name: Hunter
      threshold: 0.8
      object_config:
        objects:
          - person
        classification_type: sub_label
    Dalen:
      enabled: true
      name: Dalen
      threshold: 0.8
      object_config:
        objects:
          - person
        classification_type: sub_label
    Stephen:
      enabled: true
      name: Stephen
      threshold: 0.8
      object_config:
        objects:
          - person
        classification_type: sub_label
