apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nvr-dashboard
  namespace: nvr
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`nvr.project-solomon.ai`)
      kind: Rule
      services:
        - name: nvr-oauth2-proxy
          namespace: nvr
          port: 4180
